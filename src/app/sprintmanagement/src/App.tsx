import { Box } from "@chakra-ui/react";

const App = () => {
  return (
    <Box
      w={`100%`}
      height={`100vh`}
      display={`flex`}
      justifyContent={`center`}
      alignItems={`center`}
    >
      Hello There!
    </Box>
  );
};

export default App;
